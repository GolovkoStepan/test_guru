<%= simple_form_for [:admin, @test] do |f| %>
  <%= f.input :title %>
  <%= f.association :category, prompt: :translate %>
  <%= f.input :level %>
  <%= f.button :submit, t('links.save') %>
<% end %>
