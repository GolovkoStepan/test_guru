<tr>
  <td><%= Test.human_attribute_name('title') %> <%= test.title %></td>
  <td><%= t('.questions_count', count: test.questions.count) %></td>
  <td><%= link_to t('links.show'), admin_test_path(test) %></td>
  <td><%= link_to t('links.edit'), edit_admin_test_path(test) %></td>
  <td><%= link_to t('links.destroy'), admin_test_path(test), method: :delete, data: { confirm: t('messages.destroy') } %></td>
</tr>
