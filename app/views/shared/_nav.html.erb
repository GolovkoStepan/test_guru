<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to t('project.name'), :root, class: 'navbar-brand' %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
        <li class="nav-item">
          <%= content_tag :span, t('.greetings', username: current_user.username ), class: 'navbar-text' %>
        </li>

        <% if current_user.admin? %>
          <li class="nav-item">
            <%= link_to t('links.admin_panel'), admin_tests_path, class: 'nav-link' %>
          </li>
        <% end %>

        <li class="nav-item">
          <%= link_to t('links.user_settings'), edit_user_registration_path, class: 'nav-link' %>
        </li>
        <li class="nav-item">
          <%= link_to t('links.sign_out'), destroy_user_session_path, method: :delete, class: 'nav-link' %>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to t('links.sign_in'), new_user_session_path, class: 'nav-link' %>
        </li>
        <li class="nav-item">
          <%= link_to t('links.sign_up'), new_user_registration_path, class: 'nav-link' %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
